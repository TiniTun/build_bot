# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv
.venv-*
.uv-cache/
.ruff_cache/
.pytest_cache/

# uv.lock is intentionally tracked: the Docker build runs `uv sync --frozen`,
# so a clone without the lockfile cannot produce a reproducible image.

.vscode/
*.code-workspace
pyrightconfig.json

*.local.toml
*.local.json

# default_workspace/

default_workspace/config.user.yaml
default_workspace/config.runtime.yaml

default_workspace/.history/
default_workspace/.logs/
default_workspace/.event/
default_workspace/.secrets/
default_workspace/memories/
default_workspace/crons/

docs/

._*
.DS_Store
