[project]
name = "build-bot"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "asyncio>=4.0.0",
    "crawl4ai>=0.8.6",
    "litellm>=1.83.0",
    "pydantic>=2.12.5",
    "python-telegram-bot>=22.7",
    "pyyaml>=6.0.3",
    "rich>=14.3.3",
    "ruff>=0.15.9",
    "telegram>=0.0.1",
    "typer>=0.24.1",
    "watchdog>=6.0.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["cli", "core", "provider", "utils"]

[project.scripts]
build-bot = "cli.main:app"

[tool.ruff]
line-length = 120
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F"]
ignore = [
    "E501",  # line too long
    "F401",  # unused imports
]
